import 'package:flutter/material.dart';

const bgDark = Color(0xFF0D0F18);
const surface = Color(0xFF161925);
const surfaceCard = Color(0xFF1E2235);
const accent = Color(0xFF6C63FF);
const accentSoft = Color(0xFF8B85FF);
const textPrimary = Color(0xFFECEFF8);
const textSecondary = Color(0xFF9095B0);
const dividerColor = Color(0xFF252A40);

const statusColors = {
  'in_progress': Color(0xFF3B82F6),
  'completed': Color(0xFF22C55E),
  'cancelled': Color(0xFFEF4444),
};

const statusLabels = {
  'in_progress': 'En progreso',
  'completed': 'Completado',
  'cancelled': 'Cancelado',
};

const statusIcons = {
  'in_progress': Icons.hourglass_top_rounded,
  'completed': Icons.check_circle_rounded,
  'cancelled': Icons.cancel_rounded,
};
